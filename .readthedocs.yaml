# Read the Docs build configuration.
# Community hosting is for open source documentation; this repo is MIT licensed
# and the documented artifact is the schema and validator themselves.
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt
